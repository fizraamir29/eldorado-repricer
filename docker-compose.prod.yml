version: '3.8'

services:
  db:
    image: postgres:16-alpine
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-repricer}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-repricer_secure_pass_2026}
      POSTGRES_DB: ${POSTGRES_DB:-repricer}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-repricer}"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: always
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-repricer}:${POSTGRES_PASSWORD:-repricer_secure_pass_2026}@db:5432/${POSTGRES_DB:-repricer}
      SECRET_KEY: ${SECRET_KEY:-prod_super_secret_jwt_key_987654321}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY:-Y29uZmlndXJhdGlvbl9zZWNyZXRfa2V5X2Zvcl9mZXJuZXRf}
      ENVIRONMENT: production
      CORS_ALLOWED_ORIGINS: "*"
    depends_on:
      db:
        condition: service_healthy
    expose:
      - "8000"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: always
    environment:
      NEXT_PUBLIC_API_URL: ""
    depends_on:
      - backend
    expose:
      - "3000"

  nginx:
    image: nginx:alpine
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - certbot-etc:/etc/letsencrypt
    depends_on:
      - backend
      - frontend

volumes:
  pgdata:
  certbot-etc:
